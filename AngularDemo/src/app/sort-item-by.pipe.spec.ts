import { SortItemByPipe } from './sort-item-by.pipe';

describe('SortItemByPipe', () => {
  it('create an instance', () => {
    const pipe = new SortItemByPipe();
    expect(pipe).toBeTruthy();
  });
});
